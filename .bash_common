#-*- mode: sh -*-

export VISUAL='emacs'
export EDITOR="$VISUAL"

shopt -s autocd cdspell cdable_vars dirspell \
      extglob globstar lastpipe # failglob nocaseglob dotglob
GLOBIGNORE='.:..'
shopt -u dotglob

stty -ixon
